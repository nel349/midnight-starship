{
  "name": "@midnight-starship/game-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --mode undeployed",
    "dev:preprod": "vite --mode preprod",
    "dev:preview": "vite --mode preview",
    "build": "rm -rf ./dist && tsc && vite build --mode preview && cp -r ../contract/src/managed/starship/keys ./dist/keys && cp -r ../contract/src/managed/starship/zkir ./dist/zkir",
    "build:undeployed": "rm -rf ./dist && tsc && vite build --mode undeployed && cp -r ../contract/src/managed/starship/keys ./dist/keys && cp -r ../contract/src/managed/starship/zkir ./dist/zkir",
    "build:preprod": "rm -rf ./dist && tsc && vite build --mode preprod && cp -r ../contract/src/managed/starship/keys ./dist/keys && cp -r ../contract/src/managed/starship/zkir ./dist/zkir",
    "build:start": "npm run build && vite preview",
    "build:start:undeployed": "npm run build:undeployed && vite preview",
    "build:start:preprod": "npm run build:preprod && vite preview",
    "start": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
